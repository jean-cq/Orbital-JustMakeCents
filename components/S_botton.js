// JavaScript source code
import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Botton, Text, View } from 'react-native';

export default S_botton = () => {
    <View>
        <Botton title='Log In' onPress={() => navigation.navigate()} />
    </View>

}

const styles = StyleSheet.create({
    botton: {
        flex: 1,
        padding: 24,
        backgroundColor: "#eaeaea",
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center'
    }

});